@page
@model OnlineFashionStore.Pages.Wishlists.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Wishlist</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
          <th>
    @Html.DisplayNameFor(model => model.Wishlist[0].Client.FullName)
</th>
<th>
    @Html.DisplayNameFor(model => model.Wishlist[0].Product.Name)
</th>
<th>
    @Html.DisplayNameFor(model => model.Wishlist[0].DateAdded)
</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Wishlist) {
        <tr>
             <td>
                    @Html.DisplayFor(modelItem => item.Client.FullName)
    </td>
    <td>
                    @Html.DisplayFor(modelItem => item.Product.Name)
    </td>
    <td>
                    @Html.DisplayFor(modelItem => item.DateAdded)
    </td>

            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
